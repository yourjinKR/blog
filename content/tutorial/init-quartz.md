---
title: Quartz 시작하기
order: "2"
---
# 배포

- template 사용
- cloudflare에서 github를 통해 연결
- 특정 브랜치의 코드가 배포될때마다 자동 배포

# 기본 설정

## 패키지 설치

```shell
npm install
```

## 로컬에서 실행

```shell
npx quartz build --serve
```

