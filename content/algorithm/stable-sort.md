---
title: 안정 정렬
---
## 안정 정렬

같은 우선순위를 가지는 것끼리는 **기존의 정렬 순서를 유지**하는 정렬이다.  

> 예시: 나이순으로 정렬하되 나이가 같다면 기존 정렬 기준인 `id` 값을 기준으로 정렬한다.

### 안정 정렬의 예시

- 병합 정렬(Merge Sort)
- [[bubble-sort|버블 정렬(Bubble Sort)]]
- 삽입 정렬(Insertion Sort)
- 계수 정렬(Counting Sort)
- 팀 소트(팀 정렬, Tim sort)