
# 데이터 모델링

데이터 모델링은 단지 시스템 구현만을 위해 수행하는 태스크가 아닌, 시스템 구현을 포함한 업무형상화를 하기 위한 목적도 있다.  

## 특징

- 추상화 (모형화, 가설적) : 현실세계 일정한 형식에 맞추어 표현 (일정한 양식 표기법) 
- 단순화 : 복잡한 헌실세계 약속된 규약에 의해 제한된 표기법/언어로 표현 쉽게 이해 
- 명확화 : 누구나 이해하기 쉽게 대상의 애매모호함 제거 정확하게 현상을 기술

## 관점

- 데이터 관점 (Data, What) : 업무와 데이터 or 데이터간의 관계
- 프로세스 관점 (Process, How) : 업무가 실제 하는 일 or 무엇을 해야 하는지
- 상관 관점 (Interaction) : 업무가 처리하는 일의 방법에 따라 데이터가 받는 영향

## 주의점

- 중복 (Duplication) : 데이터베이스가 여러 장소에 같은 정보 저장하는 것 주의
- 비유연성 (Inflexibility) : 사소한 업무변화에 데이터모델 수시로 변경되면 유지보수 어려움
	→ 데이터의 정의를 데이터의 사용 프로세스와 분리
- 비일관성 (Inconsistency) : 데이터의 중복이 없어도 비일관성 발생 가능
	→ 모델링 할 때 데이터간 상호 연관관계 명확히 정의

## 단계

 데이터 모델링의 진행 3단계

```
[ 개념적 (추상) → 논리적 → 물리적 (구체) ]
```

- 개념 (계획/분석) : 추상화, 업무중심, 포괄, 전사적, EA수립 추상적
- 논리 (분석) : KEY, 속성, 관계 표현, 재사용성, 정규화 ↓
- 물리 (설계) : 데이터베이스 이식, 성능/저장 등 물리적 성격 고려 구체적

## 3단계 스키마

![[Pasted image 20260822055656.png|535]]

- 외부 스키마(사용자): 외부 스키마는 사용자나 응용 프로그래머가 개인의 입장에서 필요한 데이터베이스의 논리적 구조를 정의한다.
- **개념 스키마(설계도): 개념 스키마는 데이터베이스의 전체적인 논리적 구조로, 모든 응용 프로그램이나 사용자들이 필요로 하는 데이터를 종합한 조직 전체의 데이터베이스로 하나만 존재한다.**
	- 구조와 데이터를 통합한다, 라는 키워드가 나오면 개념 스키마
- 내부 스키마(저장소): 내부 스키마는 물리적인 저장장치 입장에서 데이터가 저장되는 방법을 기술한 것이다.

## 데이터 독립성

* 논리적 독립성 - 논리적 사상 (외부적-개념적) 을 통해 논리적 독립성 보장 - 개념 스키마가 변경되어도 외부 스키마에는 영향 X - 논리적 구조가 변경되어도 응용 프로그램에는 영향이 없음, 통합구조 변경 가능 
* 물리적 독립성 - 물리적 사상 (개념적-내부적) 을 통해 물리적 독립성 보장 - 내부스키마가 변경되어도 외부/개념 스키마는 영향 X - 응용프로그램과 개념스키마에 영향 없이 저장장치 구조변경 가능, 물리적 구조


# 엔티티

- 유무형에 따른 분류
	- 유형: 교수, 강의실, 학생
	- 개념: 수업, 보험상품
	- 사건: 수강신청, 주문
- 발생시점에 따른 분류
	- 기본(key) 엔터티 : 독립적으로 생성되는 엔터티
	- 중심(main) 엔터티 : 기본 엔터티와 행위 엔터티 중간에 존재하는 엔터티
	- 행위(active) 엔터티 : 2개 이상의 부모 엔터티로부터 발생, 비즈니스 프로세스를 실행하면서 생성되는 엔터티, 지속적 정보가 추가되고 변경되어 데이터양이 가장 많음

## 특성

> 유인관 업소

- 식별자가 있어야 한다
- 2개 이상의 인스턴스가 있어야 한다
- 속성을 가진다
- 다른 엔티티와 관계가 있다

![[Pasted image 20260822060610.png|653]]


# 관계

연관관계는 소스코드에서 멤버변수로 선언하여 사용하게 하고 의존관계는 오퍼레이션에서 유일성을 확보하는 것이 반정규화, 통합 등의 작업을 할 때 방지할 수 있는 방법이 된다.  

관계 표기법은 관계명, 관계 차수, 선택성(선택사양)의 3가지 개념으로 표현한다.  



## 식별관계 비식별 관계

![[Pasted image 20260822061005.png|402]]

![[Pasted image 20260822061024.png|405]]

![[Pasted image 20260822061105.png]]

# 데이터 무결성

![[Pasted image 20260822061216.png]]

# 정규화


### 식별자

![[Pasted image 20260822061908.png]]

![[Pasted image 20260822061849.png|474]]

![[Pasted image 20260822061959.png]]

![[Pasted image 20260822062048.png|431]]

### ERD 작성 순서

![[Pasted image 20260822062141.png]]

## 제 1정규형

![[Pasted image 20260822062231.png]]

## 제 2 정규형

![[Pasted image 20260822062328.png]]

## 제 3 정규형

![[Pasted image 20260822062419.png]]

## 함수 종속성

![[Pasted image 20260822062509.png]]

## 반정규화

성능을 위해 정규화를 하지 않는 것

- 통합
- 중복
- 파생

![[Pasted image 20260822062549.png]]

# 문자 함수

- `||`: 문자열 결합
- 예시: `'유' || '어진'` → `'유어진'`

- `CONCAT(a, b)`: 문자열 결합
- 예시: `CONCAT('유', '어진')` → `'유어진'`

- `UPPER(str)`: 영문을 대문자로 변환
- 예시: `UPPER('hello')` → `'HELLO'`

- `LOWER(str)`: 영문을 소문자로 변환
- 예시: `LOWER('HELLO')` → `'hello'`

- `LENGTH(str)`: 문자열의 글자 수 반환
- 예시: `LENGTH('어진')` → `2`

- `LENGTHB(str)`: 문자열의 바이트 수 반환
- 예시: `LENGTHB('ABC')` → `3`

- `SUBSTR(str, 시작, 길이)`: 문자열 일부 추출
- 예시: `SUBSTR('ABCDE', 2, 3)` → `'BCD'`

- `INSTR(str, 찾을문자)`: 문자의 위치 반환
- 예시: `INSTR('ABCDE', 'C')` → `3`

- `REPLACE(str, 찾을문자, 바꿀문자)`: 문자열 치환
- 예시: `REPLACE('ABCABC', 'A', 'X')` → `'XBCXBC'`

- `REPLACE(str, 찾을문자)`: 찾은 문자열 제거
- 예시: `REPLACE('ABCABC', 'A')` → `'BCBC'`

- `LTRIM(str)`: 왼쪽 공백 제거
- 예시: `LTRIM('   ABC')` → `'ABC'`

- `RTRIM(str)`: 오른쪽 공백 제거
- 예시: `RTRIM('ABC   ')` → `'ABC'`

- `TRIM(str)`: 양쪽 공백 제거
- 예시: `TRIM('   ABC   ')` → `'ABC'`

- `LTRIM(str, 문자)`: 왼쪽에서 지정 문자 제거
- 예시: `LTRIM('AAABC', 'A')` → `'BC'`

- `RTRIM(str, 문자)`: 오른쪽에서 지정 문자 제거
- 예시: `RTRIM('ABCCC', 'C')` → `'AB'`

- `LPAD(str, 길이, 문자)`: 왼쪽을 지정 문자로 채움
- 예시: `LPAD('123', 5, '0')` → `'00123'`

- `RPAD(str, 길이, 문자)`: 오른쪽을 지정 문자로 채움
- 예시: `RPAD('123', 5, '0')` → `'12300'`

- `ASCII(char)`: 문자의 ASCII 코드값 반환
- 예시: `ASCII('A')` → `65`

- `CHR(number)`: ASCII 코드값에 해당하는 문자 반환
- 예시: `CHR(65)` → `'A'`

# NULL

## NULL 관련 함수

- `NVL(a, b)`: `a`가 NULL이면 `b` 반환
- 예시: `NVL(NULL, 10)` → `10`

- `NVL2(a, b, c)`: `a`가 NULL이 아니면 `b`, NULL이면 `c` 반환
- 예시: `NVL2(NULL, 10, 20)` → `20`

- `NULLIF(a, b)`: `a = b`이면 NULL, 다르면 `a` 반환
- 예시: `NULLIF(10, 10)` → `NULL`
- 예시: `NULLIF(10, 20)` → `10`

- `COALESCE(a, b, c, ...)`: NULL이 아닌 첫 번째 값 반환
- 예시: `COALESCE(NULL, NULL, 30, 40)` → `30`

- `IS NULL`: NULL인지 확인
- 예시: `WHERE name IS NULL`

- `IS NOT NULL`: NULL이 아닌지 확인
- 예시: `WHERE name IS NOT NULL`

## NULL 핵심 규칙

- `NULL = NULL` → 참이 아님 (`UNKNOWN`)
- NULL 비교는 `= NULL`이 아니라 `IS NULL` 사용

- `10 + NULL` → `NULL`
- 일반 연산에 NULL이 포함되면 결과도 NULL

- `SUM(10, 20, NULL)` → NULL을 제외하고 계산 → `30`
- `SUM`, `AVG`, `COUNT(컬럼)` 등 집계 함수는 NULL을 제외

- `COUNT(*)` → NULL 여부와 관계없이 행 개수 계산
- `COUNT(컬럼)` → 해당 컬럼이 NULL인 행은 제외

- Oracle에서 `''`(빈 문자열)은 `NULL`로 취급

# 서브쿼리

- FROM 절 = 인라인 뷰 = 동적 뷰
- 비연관 서브쿼리가 주로 메인쿼리에 값을 제공하기 위한 목적으로 사용

# 뷰

- 뷰의 특징 3가지
	- 독립성: 원본 데이터가 바뀌더라도 수정할 필요 x
	- 편리성: 복잡한 쿼리를 미리 설정하여 편리하게 사용 가능
	- 보안성: 한정적인 데이터만 보여줄 수 있음
- 데이터를 직접 저장 x
- 인덱스 설정 불가, 처리 속도가 무조건 빨라지는 것은 아니다

# IN vs EXISTS

- IN: 목록의 값을 비교 (필터링 목적으로 주로 사용)
- EXISTS: 존재하는지 여부 판단  (서브쿼리의 여부 확인으로 확인 가능)
- NOT IN: 서브쿼리 결과에 NULL이 있다면 비교 결과가 UNKNOWN
- NOT EXISTS: 문제 없음

# 집합 연산자

- UNION: 정렬 및 중복 제거로 인해 성능이 좋지 않음
- 컬럼 갯수 동일, 데이터 타입 일치해야 사용 가능
- ORDER BY는 한번만 사용할 수 있다
- GROUP BY는 각각 사용 가능

# 공집합

- SUM은 NULL
- COUNT는 0

# 계층형 쿼리

- 피자순
- START WITH: 루트 선정
- CONNECT BY: 계층 생성 (여기에 조건절을 걸면 계층을 생성하지 음)
- WHERE: 최종 결과 필터
- `PRIOR` 키워드는 `SELECT`, `WHERE` 절에서도 사용 가능

## 관련 함수

- LEVEL: 깊이를 출력 (1부터 시작)
- CONNECT_BY_ROOT: 최상위 루트를 찾기
- CONNECT_BY_ISLEAF: 최하위 루트라면 1 반환

## 정렬

- ORDER SIBLINGS BY ID: 계층은 유지하되 형제 노드끼리 정렬

# TOP N

- ROWNUM: 정렬 전 번호가 붙음
- TOP N WITH TIES: 공동 등수에 대한 모든 데이터를 가져옴
- OFFSET N ROWS: 정렬 후 앞에서 N개 건너뛰기
- FETCH FIRST/NEXT N ROWS ONLY: 이후 N개의 행만 가져오기
	- ONLY 대신 WITH TIES를 사용한다면 마지막 요소가 동순위가 있다면 해당 행도 가져옴
	- N대신 PERCENT 사용 가능
		- PERCENT 계산시 소수점이 나오면 항상 올린다
		- 예시: 1.05 → 2

# PIVOT & UNPIVOT

행열바꾸기 기능

## PIVOT

행 → 열

```
집계함수 FOR 기준컬럼 IN 변환될 값
```

## UNPIVOT

열 → 행

```
UNPIVOT (컬럼값 FOR 구분컬럼 IN (기존열1, 기존열2))
```

## 차이점

| 비고       | PIVOT      | UNPIVOT                   |
| -------- | ---------- | ------------------------- |
| 방향       | 행 → 열      | 열 → 행                     |
| 집계 함수 사용 | 필수         | 사용 안함                     |
| NULL 처리  | 집계 시 무시    | 기본적으로 결과에서 제외 (명시적 포함 가능) |
| 핵심 요약    | 세로를 가로로 집계 | 가로를 세로로 해체                |

# 정규표현식

- `^`가 밖에 있을 때는 시작 앵커
- `^`가 `[]` 안에 있을 경우에는 부정 문자

## 함수

- REGEXP_LIKE: 다양한 정규 표현식과 함께 LIKE문 사용 가능
- REGEXP_REPLACE: 다양한 정규 표현식과 함께 문자 대체
- REGEXP_SUBSTR: 다양한 정규 표현식과 함께 문자열 자르기

# DML

## INSERT

- 삽입시 컬럼 개수가 일치, 데이터 타입이 일치, 데이터 타입 길이 일치
- 주의사항
	- NOT NULL 확인
	- PK 컬럼 또한 NOT NULL이니 확인
	- PK가 복합키일때 중복인지 잘 확인하자

> 테이블 정의서 먼저 보고 PK랑 NOT NULL 컬럼을 찾자


- 묵시적 형변환
	- 데이터타입이 안맞다면 형변환을 시도
	- 문자 → 숫자
	- 숫자 → 문자
	- 날짜 → 문자
	- 숫자 → 날짜 (불가능)
## MERGE

타켓테이블에 있다면 UPDATE, 없다면 INSERT (UPSERT)

```SQL
MERGE INTO 타겟테이블 USING 소스테이블
	ON 절 
WHEN MATCHED TEHN UPDATE 문
WHEN NOT MATCHED THEN ISERT 문 -- 생략 가능
```

# TCL

## ACID

> 원일고지만 기억, 

- 원자성(`Atomicity`)은 **트랜잭션이 완전히 수행되거나 전혀 수행되지 않아야 함**을 의미한다.
- 일관성(`Consistency`)은 트랜잭션이 **데이터베이스의 상태를 항상 일관되게 유지**해야 함을 의미한다.
- 고립성(`Isolation`)은 **동시에 실행되는 트랜잭션이 서로의 영향을 받지 않아야 함을 의미**한다.
- 지속성(`Durability`)은 트랜잭션이 성공적으로 완료되면, 그 결과가 영구적으로 저장되어야 함을 의미합니다.

## SAVEPOINT 

- 동일한 포인트를 두번 찍었다면 가장 가까운 것을 기준으로 사라짐
- 에러 주의사항
	- 커밋 이전의 세이브 포인트로 롤백한다면 에러가 발생한다
		- SAVEPOINT S1; → COMMIT → ROLLBACK TO S1; (에러 발생)

## COMMIT 

### 묵시적 커밋

자동 커밋이 발생

- 오라클에서는 DDL 후 커밋 발생
- SQL Server에서는 DDL 후 커밋 X
- TRUNCATE는 발생

# DDL

## 제약조건

- NOT NULL
- UNIQUE (NULL 여러개 가능)
- PRIMARY KEY (NOT NULL _ UNIQUE)
- CHECK 

## CASCADE

- 참조데이터까지 싹 다 삭제제
## RESRICT

- 참조 중이라면 삭제 불가

## ALTER

- 데이터가 이미 들어있다면 타입 변경 X (호환성 있는 타입끼리는 가능)
	- VARCHAR → CHAR
	- DATE → TIMESTAMP

## CTAS

테이블 복사

```
CREATE TABLE AS SELECT ~
```

제약조건은 복사되지 않음 (NOT NULL만 가져옴)

# DCL

## GRANT

## REVOKE

- REVOKE CASCADE: 아래까지 싹 다 회수
- REVOKE RESTRICT: 종속된 권한이 있으면 회수 


# 윈도우 함수

![[Pasted image 20260822030738.png]]

```
SUM(급여) OVER (PARTITION BY 부서)
```

## ORDER BY

파티션 내부의 순서를 부여함

![[Pasted image 20260822030943.png]]

## WINDOWING 절

![[Pasted image 20260822031148.png]]

![[Pasted image 20260822031122.png]]

# GROUPING

## ROLL UP

단계별 소계와 총계

![[Pasted image 20260822032011.png]]

![[Pasted image 20260822032027.png]]

## CUBE

가능한 모든 조합의 소계

> 큐브는 모든 면을 보여준다

![[Pasted image 20260822031853.png]]

![[Pasted image 20260822031933.png|442]]

## GROUPING SETS

사용자 지정

## GROUPING 

![[Pasted image 20260822034232.png]]

# 오답노트

# 1~25

1. Select List에 서브쿼리를 사용할 수 있다.
2. 산술 연산자는 하나가 NULL 이라면 값이 NULL, 집계 함수는 NULL를 제외하고 연산한다.  
3. NULL값을 조건절에 쓸 경우 IS NULL, IS NOT NULL를 사욜한다.  
4. ???
5. REPLACE(X, CHR(10))은 X라는 문자열의 줄바꿈을 제거하는 것이다.
	1. 아스키 코드로 10이 줄바꿈이다.
	2. 줄바꿈의 길이는 1이다.
	3. REPLACE(문자열, 찾을 문자열, 바꿀 문자열)
		1. 바꿀 문자열을 입력하지 않는다면 해당 문자들을 없앤다
6.  오라클에서는 날짜의 더하는 숫자는 일이다 (DATE + 1은 DATE에 하루를 더하는 것)
	1. 예시: DATE + 1/24/60 == DATE에 1분을 더한 것
7. `=` 조건만 있을 경우에는 CASE 표현식을 단순 CASE 표현식으로 바꿀 수 있다. (CASE 표현식 알아보기)
8. DECODE 함수 알아보기
9. NULLIF(표현식1, 표현식2): 표현식1과 표현식2가 같다면 NULL를 표기, 같지 않다면 표현식1을 나타낸다.
	1. 단일행 NULL 관련 함수의 종류 알아보기
10. LIKE문에서 `%` → 문자 0개 이상, `_` → 문자 정확히 1개 

# 25~50

27. group by에서도 NULL를 포함한다
28. ORDER BY의 특징
	1. 오름차순(ASC)이 기본
	2. 숫자형: 작은 값 부터
	3. 날짜형: 이른 날짜 부터
	4. 오라클: NULL값은 가장 큼
	5. SQL SERVER: NULL값은 가장 작음
29. ㅇㅇ
	1. ORDER BY는 SELECT 절에 선언되지 않는 컬럼으로도 정렬 가능하다. 
	2. `SELECT` 절이나 `ORDER BY` 절 등에서 그룹화 기준이 아닌 개별 행의 값을 그대로 가져오려고 하면 에러가 발생
30. 
31. 
32. 
33. 
34. 
35. 
36. 
37. 순수 관계 연산자: `SELECT`, `PROJECT`, `JOIN`, `DIVIDE`
38. 
39. 
40. 
41. `INNER JOIN`은 어떤 조건으로 붙여야 하는건지 명시
42. `USING` 조건절에는 별칭이나 접두사를 붙이지 않는다 (NATURAL JOIN도 마찬가지)
43. `CROSS JOIN`: 테이블간 `JOIN` 조건이 없는 경우 생길 수 있는 모든 데이터의 조합을 말한다.
44. `ON` 절에 있는 조건을 `WHERE`에 있는 조건과 헷갈리지 않도록 주의
	1. `WHERE`절은 애초에 행이 나오지 않음
	2. `ON`절은 조인만 하지 않는 것이기에 행은 있되 조인은 동작하지 않아 `NULL`과 같이 표시
45. 
46. 
47. 
48. 
49. ORACLE에서는 OUTER JOIN을 `+` 기호를 사용하여 처리할 수 있음
50. 

# 출처

https://www.youtube.com/@daynalhub